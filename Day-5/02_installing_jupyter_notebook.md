# 2. Installing Jupyter Notebook

You can install Jupyter Notebook using pip:

```bash
pip install notebook
```

To launch Jupyter Notebook, run:

```bash
jupyter notebook
```

This will open the Jupyter dashboard in your web browser.

**Tip:** Make sure your virtual environment is activated before installing.
